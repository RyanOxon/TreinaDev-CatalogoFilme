<h1 class="page-top"><%= @director.name%> </h1>

<div>
  <%= link_to "Editar diretor", edit_director_path(@director), class:"new-button" %> |
  <%= link_to "Voltar a lista", directors_path, class:"new-button", style:"margin-left: 0" %>
  <%= button_to "Deletar diretor", @director, method: :delete, class:"delete-button" %>
</div>

<div id="info-diretor">
  <%= render @director %>
 </div>

<h2>Filmes:</h2>
<%= render '/shared/list', movies: @director.movies, list: nil %>