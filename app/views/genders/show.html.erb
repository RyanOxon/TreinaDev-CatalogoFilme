<h1 class="page-top"><%= @gender.name %></h1>

<div id="gender-infos">
  <%= link_to "Editar gênero", edit_gender_path(@gender), class:"new-button" %> |
  <%= link_to "Voltar a lista", genders_path, class:"new-button", style:"margin-left: 0" %>
  <%= button_to "Deletar gênero", @gender, method: :delete, class:"delete-button" %>
</div>

<h2>Filmes:</h2>

<%= render '/shared/list', movies: @gender.movies %>
